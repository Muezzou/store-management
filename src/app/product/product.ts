export interface Product {
    id: string,
    data: Data
  }

  export interface Data {
    title: string,
    category: string,
    price: number,
    employee: string,
    description: string,
    reviews: string[]
  }

